func fib(n : int): int {
  if n == 0 then
    return 0;
  end;
  if n == 1 then
    return 1;
  end;
  return fib(n - 1) + fib(n - 2);
} end


func main(): int {
  writeInt(fib(13));
  return 0;
} end
